# 핵심 동작 패키지 (RTMPose + 딥러닝)
ultralytics==8.3.221
torch==2.9.0

# 영상처리/포즈 추정 시각화
opencv-python==4.11.0.86
numpy==1.26.4
matplotlib==3.10.7
pillow==12.0.0

# MediaPipe 연동 필요시 (얼굴/핸드/추가 키포인트 쓸 때만)
mediapipe==0.10.21

# 아래는 옵션 (특정 코드/함수 추가시만 필요한 라이브러리)
PyYAML==6.0.3
requests==2.32.5
python-dateutil==2.9.0.post0

# (import 안 하거나 코드에 직접 안 쓰면 삭제 가능)
#opencv-contrib-python==4.11.0.86  ← opencv-python만으로 충분하면 삭제 가능
#torchvision==0.24.0                ← TorchVision 기능 안 쓰면 삭제
